extends layout

block content

  h1 #{title}

    p Do you really want to delete this Book Instance?

    form(method='POST')
        div.form-group
            input#bookinstancerid.form-control(type='hidden', name='bookinstancerid', value=instance._id )

            button.btn.btn-primary(type='submit') Delete